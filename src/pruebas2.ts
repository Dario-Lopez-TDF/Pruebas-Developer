let numero: number = Number(prompt("Ingrese un numero"));

switch (numero) {
  case 1:
    if (numero > 0 && numero % 2 === 0) {
    }
    console.log(numero + "Su numero es PAR");
    break;

  case 2:
    if (numero > 0 && numero % 2 === 1) {
    }
    console.log(numero + "Su numero es IMPAR");
    break;

  case 3:
    if (numero == 0) {
    }
    console.log(numero + "Su numero es CERO");
    break;

  default:
    console.log(numero + " " + "Su numero es CERO");
    break;
}

switch (key) {
  case value:
    break;

  default:
    break;
}

// CLASE DE MARTES 03/05/2022  - EJERCICIOS DE LA CLACE Y PRACTICAS
